{
  "name": "goodreads-json-api",
  "version": "0.3.0",
  "description": "This package fetches data from GoodReads API and returns a it as JSON object",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Artem Rosnovsky",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.2.0",
    "xml-js": "^1.6.8"
  }
}
